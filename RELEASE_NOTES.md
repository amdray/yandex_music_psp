# История релизов YMPSP

## v0.2

### Вход и аккаунт
- Вход по коду с телефона: пункт «Профиль», код вводится на `ya.ru/device`, токен сохраняется сам.
- QR-код со страницей ввода рядом с кодом.
- Выход из аккаунта квадратом (`[]`) на экране аккаунта (чистит токен и кэш).
- Без токена приложение больше не висит на заставке — открывается гостевое меню.
- После входа — сразу в главное меню.

### Звук
- Эквалайзер 7 полос (60/150/400/1k/2.4k/6k/15k): пресеты Выкл/Рок/Поп/Джаз/Классика/Бас/Высокие/Вокал/Свой, свой профиль настраивается в меню.
- Переключение профилей: `L`/`R` на плеере (кнопка `♪` опрашивается не везде).
- Предусиление до +18 дБ: `VOL+` на максимуме системной громкости, `VOL−` сливает.
- Качество MP3 192/320 (действует со следующего трека), формат строкой `MP3 192 kbps · 44.1 kHz`.
- Удержание `SELECT` — меню (качество/эквалайзер/усиление), короткое — подсказка.

### Музыка
- Лайки треугольником (`△`) на плеере, сердце меняет цвет.
- Альбомы: список лайкнутых с обложками, открытие кнопкой `X`, игра из карточки исполнителя; `□` в карточке играет топ треков.
- Моя волна: Rotor-сессия, автодогрузка пачек, отчётность серверу (старт/скипы/лайки).
- Поиск по каталогу с системной клавиатуры.
- Продолжение воспроизведения после перезапуска (открывает плеер, круг возвращает в меню).
- Перемотка удержанием `←`/`→` по 10 секунд (CBR).

### Интерфейс и техдолг
- Экран «Помощь» со всеми кнопками.
- Тост-подсказки внизу экрана, мигающая стрелка загрузки.
- Обрезка длинных названий (бегущая строка не лезет на обложку и за край).
- Автоматическая чистка кэша (32 свежих трека, 150 обложек).
- Перепроверка сети после сна экрана + сторож зависшего воспроизведения.
- Большие списки открываются частями (первые строки сразу, хвост в фоне).
